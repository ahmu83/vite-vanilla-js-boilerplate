{
  "name": "vite-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "watch": "concurrently \"vite build --watch\" \"sass --watch assets/sass/styles.scss:assets/css/styles.css --style compressed\"",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "alpinejs": "^3.10.5"
  }
}